# PostgreSQL connection string pointing to the inventory database.
# In Docker Compose this will be the internal service URL, e.g.:
# DATABASE_URL=postgresql://user:password@postgres:5432/inventory_db
DATABASE_URL=postgresql://user:password@localhost:5432/inventory_db

# HTTP port for this connector (each connector runs on its own port)
PORT=8001
