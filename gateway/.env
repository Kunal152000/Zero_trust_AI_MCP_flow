# PostgreSQL connection string (audit DB + RBAC policy tables)
DATABASE_URL=postgresql://user:password@localhost:5432/mcp_gateway

# Secret used to verify HS256 JWTs issued by your identity provider
JWT_SECRET=change_me_to_a_strong_random_secret

# Gateway HTTP port
PORT=3000
